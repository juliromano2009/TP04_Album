@for(int i = 1; i <= ViewBag.CantSelecciones; i++)
{
    <section>
        <h1>@ViewBag.NombreSelecciones[i]</h1>
        @for(int x = 1; x <= 5; x++)
        {
            
        }
        
</section>
}

